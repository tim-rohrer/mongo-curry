import { describe } from 'riteway';

// Just here to pass the test stage for now.
describe('nothing', async assert => {

	assert({
		given: 'nothing',
		should: 'pass the test',
		actual: true,
		expected: true
	});
});
